<?php
/**
 * Authors class
 *
 * @package erikdmitchell\bcmigration\authors
 * @since   0.3.0
 * @version 0.1.0
 */

namespace erikdmitchell\bcmigration\authors;

/**
 * Authors class
 */
class Authors {


// $args = [
//     'post_type' => 'blog',
//     'meta_key'  => 'authors',
//     'posts_per_page' => -1,
// ];

// $query = new WP_Query( $args );

// $matching_posts = [];

// foreach ( $query->posts as $post ) {
//     $authors = get_post_meta( $post->ID, 'authors', true );

//     if ( is_array( $authors ) && in_array( '93391', $authors ) ) {
//         $matching_posts[] = $post->ID;
//     }
// }		
// print_r( $matching_posts );


// $args = [
//     'post_type' => 'resource-library',
//     'meta_key'  => 'authors',
//     'posts_per_page' => -1,
// ];

// $query = new WP_Query( $args );

// $matching_posts = [];

// foreach ( $query->posts as $post ) {
//     $authors = get_post_meta( $post->ID, 'authors', true );

//     if ( is_array( $authors ) && in_array( '188204', $authors ) ) {
//         $matching_posts[] = $post->ID;
//     }
// }		
// print_r( $matching_posts );
}